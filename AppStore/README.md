# App Store Submission Placeholders

This folder tracks release assets and metadata that should be finalized before first submission.

## Current Placeholder Status

- App icon: implemented in `PeriMenoApp/Resources/Assets.xcassets/AppIcon.appiconset`.
- Privacy policy URL: placeholder in `AppConstants.privacyPolicyURL`.
- Support URL: placeholder in `AppConstants.supportURL`.
- Marketing URL: placeholder in `AppConstants.marketingURL`.
- Screenshots: plan exists in `AppStore/Screenshots/screenshot-plan.md`; final screenshots still need to be captured.
- Metadata: checklist exists in `AppStore/Metadata/metadata-checklist.md`.

## Required Before Upload

- Replace all `https://example.com/...` values with real public URLs.
- Replace `support@example.com` with a monitored support inbox.
- Verify App Store Connect subscription products match StoreKit product IDs.
- Capture final screenshots on App Store-required device sizes.
- Review privacy nutrition labels against the implemented app behavior.
